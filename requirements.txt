# Core runtime dependencies (Python 3.10+)
rdflib>=6.3,<8.0
pydantic>=2.0,<3.0

# Optional extras (install as needed):
#   pip install -e ".[validation]" — pyshacl, brickschema (SHACL conformance)
#   pip install -e ".[llm]"        — ollama (LLM-driven topology via --llm flag)
